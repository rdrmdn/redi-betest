describe("User Repository Spec", () => {
    test("Successfully find all user from database", async () => {
        expect(true).toEqual(true);
    }); 

    test("Successfully find one user from database", async () => {
        expect(true).toEqual(true);
    }); 

    test("Successfully insert user to database", async () => {
        expect(true).toEqual(true);
    });

    test("Successfully update user in database", async () => {
        expect(true).toEqual(true);
    });

    test("Successfully delete user in database", async () => {
        expect(true).toEqual(true);
    });

    test("Update user should be return undefined if it fails to update", async () => {
        expect(true).toEqual(true);
    });

    test("Delete user should be return undefined if it fails to delete", async () => {
        expect(true).toEqual(true);
    });
});
