describe("User Service Spec - User Delete", () => {
    test("Successfully delete exist user", async () => {
        expect(true).toEqual(true);
    });

    test("Delete user should be throw `Not Found` error if user not found", async () => {
        expect(true).toEqual(true);
    });
});
